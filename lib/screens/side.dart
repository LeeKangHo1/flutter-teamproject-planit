import 'package:flutter/material.dart';

// 과제 없을 때 화면 임시로 여기에
class Side extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("side")),
    );
  }
}
